#include <string>
#include <UserConfig.h>

namespace UserConfig
{
    const char *ssid = "WiFi SSID";
    const char *pass = "WiFi Password";
    const char *token = "Access Token";
} // namespace UserConfig
